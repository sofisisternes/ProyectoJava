export const card = (props)=>{
    let article=document.createElement("article")
    article.setAttribute()
    article.innerHTML =
`
<img src= ${thumbnail} alt="producto ropa ${nombre}">
<h5>${nombre}</h5>
<p>${precio}</p>
<button>
`
}